/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MpsPipe } from './mps.pipe';

describe('MpsPipe', () => {
  it('create an instance', () => {
    const pipe = new MpsPipe();
    expect(pipe).toBeTruthy();
  });
});
